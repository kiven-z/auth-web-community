---
description: 内部可信数据全信任；禁止用白名单/静默过滤藏 bug；仅用户输入须校验
alwaysApply: true
---

# 内部数据信任

## 原则

1. **用户填写**（表单、URL query、上传内容、第三方回调体等）→ 须校验 / 清洗。
2. **自己人的数据**（本仓 Pinia/Store 状态、模块内契约对象、服务端已按契约落库且由本系统写出的配置袋、内部 DTO 往返）→ **全信任，直接使用**。
3. **禁止**用字段白名单、静默丢弃、兜底默认值把错误藏掉

## 禁止

- 对本仓 Store / 内部 `pick` 一份 key 列表「防万一」
- 对本人偏好、内部配置 JSON 做「只认白名单字段」的二次裁剪（除非契约明确是「多来源合并且形状不一致」）
- `catch` 后吞掉、或过滤未知字段导致加字段不同步且无编译期反馈
