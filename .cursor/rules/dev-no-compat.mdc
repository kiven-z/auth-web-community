---
description: 开发期纪律：禁止兼容层与补丁；须重构或删重写（指标抽取见 refactor-discipline.mdc）
alwaysApply: true
---

# 开发期变更纪律

当前为**开发阶段**，无对外兼容义务。

## 基本原则

1. **须**修正根因；**禁止**以补丁掩盖问题。
2. 旧实现可被替代时，**须**删除旧路径并改齐全部调用方。
3. 抽象或边界错误时，**须**删重写；**禁止**外层 compat 封装。
4. 无引用旧代码**须**立即删除；**禁止** `@Deprecated`、开关或注释延期保留。
5. 局部错误 → 模块内重构；抽象错误 → 删重写；调用分散 → 先收敛入口再删旧。

## 禁止

双轨 API/字段、别名导出、Legacy 适配层、`if (legacy)` 兜底、新读旧回落、以「兼容/暂时/后续删除」维持旧逻辑。

## 例外

外部依赖降级（须注明外部原因）、一次性迁移脚本、产品并存能力（非历史债务）。

## 自检

- [ ] 未为旧调用保留分支、别名、adapter
- [ ] 旧实现已删，调用方已齐
- [ ] 无「兼容」「暂时」「后续删除」表述
